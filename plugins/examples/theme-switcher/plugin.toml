[plugin]
name = "theme-switcher"
version = "0.1.0"
runtime = "frontend"

[plugin.metadata]
description = "Quick theme switching with live preview"
author = "Scarab Team"
license = "MIT"

[hooks]
on_load = true

[config]
default_theme = "dracula"
